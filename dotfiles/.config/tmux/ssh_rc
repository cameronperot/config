#!/usr/bin/env bash

# Install as ~/.ssh/rc to refresh the remote-only agent symlink on SSH login
if [ -z "${container:-}" ] && [ -n "${SSH_AUTH_SOCK:-}" ] && [ "$SSH_AUTH_SOCK" != "$HOME/.ssh/ssh_auth_sock" ]; then
    ln -sfn -- "$SSH_AUTH_SOCK" "$HOME/.ssh/ssh_auth_sock"
fi
